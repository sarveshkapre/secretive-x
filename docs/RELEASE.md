# RELEASE

## Process
1. Ensure `make check` passes locally.
2. Update `CHANGELOG.md` with shipped changes.
3. Bump version in `pyproject.toml`.
4. Tag `vX.Y.Z` and push.
5. Create a GitHub Release with notes from the changelog.
